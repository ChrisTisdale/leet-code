{
    "version": "2.0.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Test",
            "program": "${cmake.testProgram}",
            "args": [ "${cmake.testArgs}" ]
        }
    ]
}